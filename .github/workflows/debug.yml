name: Test SSH Banner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Read SSH banner
        run: |
          timeout 10 bash -c 'cat < /dev/null > /dev/tcp/177.44.248.36/22'
