name: Deploy
on:
  workflow_dispatch
  #push:
  #  branches:
  #    - homologacao
  #    - producao

jobs:
  deploy-homologacao:
    if: github.ref == 'refs/heads/homologacao'
    runs-on: ubuntu-latest

    steps:
      - name: Deploy Homologação
        run: |
          curl -X POST \
            -H "x-secret: ${{ secrets.DEPLOY_SECRET }}" \
            http://177.44.248.36:9000/hooks/deploy-hml
    
#      - name: Descobrir IP do runner
#        run: |
#          curl https://api.ipify.org
#    
#      - name: Executar script remoto
#        uses: appleboy/ssh-action@v1.2.0
#        with:
#          host: ${{ secrets.SERVER_HOST }}
#          username: ${{ secrets.SERVER_USER }}
#          key: ${{ secrets.SERVER_SSH_KEY }}
#          script: |
#            /home/univates/gerencia_configuracao/comandos/levantar_homologacao.sh
